{% import '_macros/_components' as renderComponent %}

{% extends "_layout" %}

{% block content %}

	{{
		renderComponent.navigation1({

		})
	}}

	{{
		renderComponent.hero1({

		})
	}}

	{{
		renderComponent.navigation2({

		})
	}}

	{{
		renderComponent.hero2({

		})
	}}

	{{
		renderComponent.navigation3({

		})
	}}

	{{
		renderComponent.hero3({

		})
	}}

	{{
		renderComponent.navigation4({

		})
	}}

	{{
		renderComponent.hero4({

		})
	}}

	{{
		renderComponent.navigation5({

		})
	}}

	{{
		renderComponent.hero5({

		})
	}}


	{{
		renderComponent.navigation6({

		})
	}}

	{{
		renderComponent.hero6({

		})
	}}

	<script>
	  var template = 'page';
	</script>

{% endblock %}
