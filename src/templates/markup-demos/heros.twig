{% import 'authenticamplify/_macros/_components' as renderComponent %}

{% extends "_layout" %}

{% block content %}

	{{
		renderComponent.hero1({

		})
	}}

	{{
		renderComponent.hero2({

		})
	}}

	{{
		renderComponent.hero3({

		})
	}}

	{{
		renderComponent.hero4({

		})
	}}

	{{
		renderComponent.hero5({

		})
	}}

	{{
		renderComponent.hero6({

		})
	}}

	<script>
	  var template = 'page';
	</script>

{% endblock %}
